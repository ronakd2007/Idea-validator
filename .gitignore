node_modules/
dist/
.env
.env.local
*.db
*.db-journal
.next/
.claude/
backend/prisma/migrations/
graphify-out/
*.tsbuildinfo

# Database dumps contain respondent emails and every survey answer — they must
# never enter git history. The connection string file is credentials.
backups/
.env.production.local
backup.log
_seed_validators_tmp.ts
